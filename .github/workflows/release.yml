name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
      - name: Restore
        run: dotnet restore Jellyfin.Plugin.SubX/Jellyfin.Plugin.SubX.csproj
      - name: Publish
        run: dotnet publish Jellyfin.Plugin.SubX/Jellyfin.Plugin.SubX.csproj -c Release -o artifacts/publish
      - name: Prepare package
        run: |
          mkdir -p artifacts/package
          cp -R artifacts/publish/* artifacts/package/
          cd artifacts/package
          zip -r ../../Jellyfin.Plugin.SubX-${GITHUB_REF_NAME}.zip .
      - name: Generate repository manifest
        run: |
          mkdir -p artifacts/repository
          CHECKSUM=$(md5sum Jellyfin.Plugin.SubX-${GITHUB_REF_NAME}.zip | awk '{print toupper($1)}')
          export CHECKSUM
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          template = Path("manifest.template.json")
          output = Path("artifacts/repository/manifest.json")

          data = json.loads(template.read_text(encoding="utf-8"))
          data[0]["versions"][0]["checksum"] = os.environ["CHECKSUM"]
          output.write_text(json.dumps(data, indent=2) + "\n", encoding="utf-8")
          PY
          cp Jellyfin.Plugin.SubX/thumb.png artifacts/repository/thumb.png
      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Jellyfin.Plugin.SubX-${{ github.ref_name }}.zip
            artifacts/repository/manifest.json
            artifacts/repository/thumb.png
      - name: Publish manifest to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: artifacts/repository
